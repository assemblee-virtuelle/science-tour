Bonjour {{ project.creator|raw }} !

L'utilisateur {{ author.username|raw }} vient de créer une nouvelle discussion {% if chat.private %}privée {% endif %}dans votre projet "{{ project.title|raw }}".

{{ url('tst_project', {'id': project.id, 'tab': 'chats'}) }}


L'équipe du Science Tour.